// SPDX-License-Identifier: MIT
pragma solidity ^0.6.10;

import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/QuickSort.sol";

contract TestQuickSort {
/*  
  // The address of the voting contract to be tested
  Voting voting = Voting(DeployedAddresses.Voting());

  // The id of the voter that will be used for testing
  uint expectedVoterId = 8;

  //The expected owner of adopted pet is this contract
  address expectedAdopter = address(this);
  
  // Testing the unadopt() function
  function testCannotVoteTwice() public {
    
    voting.addCandidate("John", "Labor");
    voting.addCandidate("Tony", "Liberal");
    
    voting.vote("yeh", 1);
    
    //bool r;
    //(r, ) = address(this).call(abi.encodePacked(voting.vote("yeh", 2).selector));
    //Assert.isFalse(r, "If this is true, something is broken!");
  }
  
*/
  
}
